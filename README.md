# spring-roomescape-member

## 기능 목록

### 예약 관리

[사이클 1]

- 사용자는 이름, 날짜 ID, 시간 ID, 테마 ID를 입력해 예약을 생성한다.
- 예약 생성 시 이름이 비어 있으면 예외를 반환한다.
- 예약 생성 시 날짜, 시간, 테마 값이 누락되면 예외를 반환한다.
- 관리자는 전체 예약 목록을 조회한다.
- 관리자는 예약 ID로 예약을 삭제한다.

[사이클 2]

- 사용자는 자신의 이름을 통해 예약 목록을 조회한다.
- 사용자가 본인의 예약을 취소할 수 있다.
    - 예약일 하루 전 자정까지 예약을 취소할 있다.
- 사용자가 본인의 예약 날짜, 시간을 변경할 수 있다.
    - 예약일 하루 전 자정까지 예약 날짜, 시간을 변경할 수 있다.
    - 변경하고자 하는 날짜, 시간에 이미 예약이 있을 경우에는 변경할 수 없다.
- 지나간 날짜, 시간에 대한 예약 생성을 시도하면 예외를 반환한다.
- 같은 날짜, 시간, 테마에 이미 예약이 존재하면 중복 예약으로 간주하여 예외를 반환한다.

### 예약 날짜 관리

- 사용자는 예약 가능한 날짜 목록을 조회한다.
- 관리자는 전체 예약 날짜 목록을 조회한다.
- 관리자는 예약 날짜를 추가한다.
- 관리자는 예약 날짜를 삭제한다.
- 존재하지 않는 날짜를 삭제하려 하면 예외를 반환한다.
- 이미 예약에 사용 중인 날짜를 삭제하려 하면 예외를 반환한다.

### 예약 시간 관리

- 사용자는 특정 날짜와 테마에 대한 예약 가능 시간 목록을 조회한다.
- 관리자는 전체 예약 시간 목록을 조회한다.
- 관리자는 예약 시간을 추가한다.
- 관리자는 예약 시간을 삭제한다.
- 예약 시간 입력이 없거나 형식이 올바르지 않으면 예외를 반환한다.
- 존재하지 않는 시간을 삭제하려 하면 예외를 반환한다.
- 이미 예약에 사용 중인 시간을 삭제하려 하면 예외를 반환한다.

### 테마 관리

- 사용자는 전체 테마 목록을 조회한다.
- 사용자는 예약 인기 테마 랭킹을 조회한다.
- 관리자는 전체 테마 목록을 조회한다.
- 관리자는 테마를 추가한다.
- 관리자는 테마를 삭제한다.
- 예약 생성 시 테마 값이 누락되면 예외를 반환한다.
- 존재하지 않는 테마를 삭제하려 하면 예외를 반환한다.
- 이미 예약에 사용 중인 테마를 삭제하려 하면 예외를 반환한다.

### 공통 예외 처리

[사이클 1]

- 관리자 API는 `X-ADMIN-TOKEN` 헤더가 올바르지 않으면 `401 Unauthorized`를 반환한다.
- 비즈니스 예외는 `{"code": "...", "message": "..."}` 형식으로 반환한다.
- 처리하지 못한 서버 예외는 공통 에러 응답으로 반환한다.

[사이클 2]

- 행동까지 명시한 에러 응답 객체로 수정한다.
- 비즈니스 예외, 잘못된 요청은 4XX로 예외 처리한다.
- 예측 못한 예외, DB 예외와 같은 서버 책임의 에러는 5XX로 예외 처리한다.
- 에러 메시지에 개인정보나 내부 구현 정보가 포함될 수 있다면 추상화해서 반환한다.

## 공통 에러 응답 형식 및 에러 코드

API 요청 처리 중 에러가 발생한 경우, 다음과 같은 형식의 JSON 응답을 반환한다.

```json
{
  "code": "RESERVATION_TIME_NOT_EXIST",
  "message": "지정한 식별자에 해당하는 예약 시간 엔티티를 찾을 수 없습니다.",
  "action": "요청한 시간 ID의 유효성 및 DB 존재 여부를 확인하십시오."
}
```

### 상세 에러 코드 목록

#### 공통/시스템 에러 (RoomescapeErrorCode)

| 상태 코드 | 에러 코드                        | 메시지                                              |
|-------|------------------------------|--------------------------------------------------|
| `400` | `BAD_REQUEST`                | 요청 파라미터 혹은 문법이 유효하지 않습니다.                        |
| `400` | `INVALID_JSON_FORMAT`        | 요청 바디의 JSON 형식이 올바르지 않거나 역직렬화에 실패했습니다.           |
| `400` | `INVALID_PARAMETER_TYPE`     | 요청 파라미터의 데이터 타입이 기대하는 타입과 일치하지 않습니다.             |
| `400` | `MISSING_REQUIRED_PARAMETER` | 필수 쿼리 파라미터가 누락되었습니다.                             |
| `400` | `MISSING_PATH_VARIABLE`      | URL 경로 변수가 누락되었거나 형식이 올바르지 않습니다.                 |
| `400` | `INVALID_INPUT_VALUE`        | 도메인 유효성 검증 제약 조건을 위반했습니다.                        |
| `400` | `DATA_INTEGRITY_VIOLATION`   | 데이터베이스 무결성 제약 조건(Unique, Foreign Key 등)을 위반했습니다. |
| `404` | `NOT_FOUND`                  | 요청한 리소스 혹은 엔드포인트를 찾을 수 없습니다.                     |
| `405` | `METHOD_NOT_ALLOWED`         | 해당 엔드포인트에서 지원하지 않는 HTTP 메서드입니다.                  |
| `500` | `INTERNAL_SERVER_ERROR`      | 처리되지 않은 서버 내부 오류가 발생했습니다.                        |
| `500` | `INVALID_GENERATED_KEY`      | 데이터베이스에서 생성된 식별자(PK)를 조회할 수 없습니다.                |

#### 인증 에러 (AdminErrorCode)

| 상태 코드 | 에러 코드          | 메시지                      |
|-------|----------------|--------------------------|
| `401` | `UNAUTHORIZED` | 인증 정보가 누락되었거나 유효하지 않습니다. |

#### 예약 에러 (ReservationErrorCode)

| 상태 코드 | 에러 코드                       | 메시지                                          |
|-------|-----------------------------|----------------------------------------------|
| `400` | `INVALID_RESERVATION_NAME`  | 예약자 성명 데이터가 유효하지 않습니다.                       |
| `400` | `INVALID_RESERVATION_DATE`  | 예약 날짜 식별자 혹은 데이터가 누락되었습니다.                   |
| `400` | `RESERVATION_CANNOT_CANCEL` | 비즈니스 제약 조건에 의해 취소가 불가능한 상태입니다. (당일 혹은 과거 예약) |
| `400` | `RESERVATION_CANNOT_UPDATE` | 비즈니스 제약 조건에 의해 수정이 불가능한 상태입니다. (당일 혹은 과거 예약) |
| `404` | `RESERVATION_NOT_FOUND`     | 지정한 식별자에 해당하는 예약 엔티티를 찾을 수 없습니다.             |
| `409` | `RESERVATION_DUPLICATED`    | 동일한 시간대에 중복된 예약 엔티티가 존재합니다.                  |

#### 예약 날짜 에러 (ReservationDateErrorCode)

| 상태 코드 | 에러 코드                         | 메시지                                             |
|-------|-------------------------------|-------------------------------------------------|
| `400` | `INVALID_RESERVATION_DATE`    | 예약 날짜 데이터가 유효하지 않거나 누락되었습니다.                    |
| `400` | `TODAY_NOT_MODIFIED`          | 당일 예약은 수정 및 취소가 불가능합니다.                         |
| `404` | `RESERVATION_DATE_NOT_EXIST`  | 지정한 식별자에 해당하는 예약 날짜 엔티티를 찾을 수 없습니다.             |
| `409` | `RESERVATION_DATE_IN_USE`     | 외래 키 제약 조건으로 인해 삭제가 불가능합니다. (해당 날짜를 참조하는 예약 존재) |
| `409` | `RESERVATION_DATE_DUPLICATED` | 동일한 날짜 값을 가진 엔티티가 이미 존재합니다.                     |

#### 예약 시간 에러 (ReservationTimeErrorCode)

| 상태 코드 | 에러 코드                         | 메시지                                             |
|-------|-------------------------------|-------------------------------------------------|
| `400` | `INVALID_RESERVATION_TIME`    | 예약 시간 식별자 정보가 누락되었습니다.                          |
| `400` | `PAST_TIME_NOT_ALLOWED`       | 도메인 정책 위반: 과거 시점의 시간 데이터를 등록할 수 없습니다.           |
| `404` | `RESERVATION_TIME_NOT_EXIST`  | 지정한 식별자에 해당하는 예약 시간 엔티티를 찾을 수 없습니다.             |
| `409` | `RESERVATION_TIME_IN_USE`     | 외래 키 제약 조건으로 인해 삭제가 불가능합니다. (해당 시간을 참조하는 예약 존재) |
| `409` | `RESERVATION_TIME_DUPLICATED` | 동일한 시간 값을 가진 엔티티가 이미 존재합니다.                     |

#### 테마 에러 (ThemeErrorCode)

| 상태 코드 | 에러 코드                   | 메시지                                             |
|-------|-------------------------|-------------------------------------------------|
| `400` | `INVALID_THEME`         | 테마 엔티티 식별자 정보가 누락되었습니다.                         |
| `400` | `INVALID_THEME_NAME`    | 테마 명칭 데이터가 유효하지 않습니다.                           |
| `400` | `INVALID_THEME_CONTENT` | 테마 설명 데이터가 유효하지 않습니다.                           |
| `400` | `INVALID_THEME_URL`     | 테마 포스터 URL 데이터가 유효하지 않거나 형식이 올바르지 않습니다.         |
| `404` | `THEME_NOT_EXIST`       | 지정한 식별자에 해당하는 테마 엔티티를 찾을 수 없습니다.                |
| `409` | `THEME_IN_USE`          | 외래 키 제약 조건으로 인해 삭제가 불가능합니다. (해당 테마를 참조하는 예약 존재) |

## 자기 API 명세

### 공통 규칙

- Base URL: `/`
- 관리자 API 헤더: `X-ADMIN-TOKEN: {admin-token}`
- Content-Type: `application/json`

### 화면 경로

#### `GET /`

- 설명: 사용자 예약 페이지 반환
- 응답: `times` 뷰 렌더링

#### `GET /admin`

- 설명: 관리자 페이지 반환
- 응답: `admin` 뷰 렌더링

### 예약

#### `GET /admin/reservations`

- 설명: 전체 예약 조회
- 인증: 관리자
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "name": "보예",
    "date": "2026-05-01",
    "time": {
      "id": 1,
      "startAt": "10:00"
    },
    "theme": {
      "id": 1,
      "name": "공포",
      "content": "오금이 저리는 공포입니다.",
      "url": "/themes/scary"
    }
  }
]
```

#### `POST /reservations`

- 설명: 예약 생성
- 요청 본문

```json
{
  "name": "쿠키",
  "dateId": 1,
  "timeId": 2,
  "themeId": 3
}
```

- 응답 `201 Created`

```json
{
  "id": 29,
  "name": "쿠키",
  "date": "2026-05-01",
  "time": "11:00",
  "theme": {
    "name": "청춘물",
    "content": "학교 배경인 테마 입니다.",
    "url": "/themes/youth"
  }
}
```

#### `DELETE /admin/reservations/{id}`

- 설명: 관리자 권한으로 예약 삭제
- 인증: 관리자
- 응답 `204 No Content`

#### `GET /reservations?name={name}`

- 설명: 사용자 이름으로 예약 목록 조회
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "name": "보예",
    "date": "2026-05-05",
    "time": {
      "id": 1,
      "startAt": "10:00"
    },
    "theme": {
      "id": 1,
      "name": "공포",
      "content": "오금이 저리는 공포입니다.",
      "url": "/themes/scary"
    }
  }
]
```

#### `DELETE /reservations/{id}`

- 설명: 사용자 본인 예약 취소 (당일 예약은 취소 불가)
- 응답 `204 No Content`

#### `PATCH /reservations/{id}`

- 설명: 사용자 본인 예약 날짜 및 시간 변경
- 요청 본문

```json
{
  "dateId": 1,
  "timeId": 2
}
```

- 응답 `200 OK`

```json

{
  "id": 1,
  "name": "보예",
  "date": "2026-05-05",
  "time": {
    "id": 1,
    "startAt": "10:00"
  },
  "theme": {
    "id": 1,
    "name": "공포",
    "content": "오금이 저리는 공포입니다.",
    "url": "/themes/scary"
  }
}
```

### 예약 날짜

#### `GET /reservation-dates`

- 설명: 사용자용 예약 날짜 목록 조회
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "playDay": "2026-05-01"
  }
]
```

#### `GET /admin/reservation-dates`

- 설명: 관리자용 예약 날짜 목록 조회
- 인증: 관리자
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "playDay": "2026-05-01"
  }
]
```

#### `POST /admin/reservation-dates`

- 설명: 예약 날짜 추가
- 인증: 관리자
- 요청 본문

```json
{
  "playDay": "2026-05-10"
}
```

- 응답 `201 Created`

```json
{
  "id": 10,
  "playDay": "2026-05-10"
}
```

#### `DELETE /admin/reservation-dates/{id}`

- 설명: 예약 날짜 삭제
- 인증: 관리자
- 응답 `204 No Content`

### 예약 시간

#### `GET /times?themeId={themeId}&dateId={dateId}`

- 설명: 특정 테마와 날짜의 예약 가능 시간 조회
- 응답 `200 OK`

```json
[
  {
    "timeId": 1,
    "startAt": "10:00",
    "available": false
  },
  {
    "timeId": 2,
    "startAt": "11:00",
    "available": true
  }
]
```

#### `GET /admin/times`

- 설명: 전체 예약 시간 목록 조회
- 인증: 관리자
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "startAt": "10:00"
  }
]
```

#### `POST /admin/times`

- 설명: 예약 시간 추가
- 인증: 관리자
- 요청 본문

```json
{
  "startAt": "18:00"
}
```

- 응답 `201 Created`

```json
{
  "id": 6,
  "startAt": "18:00"
}
```

#### `DELETE /admin/times/{id}`

- 설명: 예약 시간 삭제
- 인증: 관리자
- 응답 `204 No Content`

### 테마

#### `GET /themes`

- 설명: 전체 테마 목록 조회
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "name": "공포",
    "content": "오금이 저리는 공포입니다.",
    "url": "/themes/scary"
  }
]
```

#### `GET /themes/rank`

- 설명: 인기 테마 랭킹 조회
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "name": "공포",
    "url": "/themes/scary"
  }
]
```

#### `GET /admin/themes`

- 설명: 관리자용 테마 목록 조회
- 인증: 관리자
- 응답 `200 OK`

```json
[
  {
    "id": 1,
    "name": "공포",
    "content": "오금이 저리는 공포입니다.",
    "url": "/themes/scary"
  }
]
```

#### `POST /admin/themes`

- 설명: 테마 추가
- 인증: 관리자
- 요청 본문

```json
{
  "name": "추리",
  "content": "단서를 조합해 탈출하는 테마입니다.",
  "url": "/themes/detective"
}
```

- 응답 `201 Created`

```json
{
  "id": 13,
  "name": "추리",
  "content": "단서를 조합해 탈출하는 테마입니다.",
  "url": "/themes/detective"
}
```

#### `DELETE /admin/themes/{id}`

- 설명: 테마 삭제
- 인증: 관리자
- 응답 `204 No Content`
