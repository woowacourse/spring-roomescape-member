# API는 어떻게 설계해야 할까?

REST API를 설계할 때 UI가 어떻게 되는지가 아니라. 도메인 관계가 어떻게 되어 있는지를  보자. 예를 들어 방탈출 시나리오면 테마 안에 예약 가능한 시간과 이미 예약된 시간이 존재할 것이다. 
`/times`를 필터링하는 방식 역시 관리자 입장에서 시간 리소스에서 필터링을 하지 않을 것이다. 예약이라면 몰라도.

리소스 설계 시 사용자의 유즈케이스를 추측하지 말고 실제 도메인이 어떻게 돌아가는지를 보자.