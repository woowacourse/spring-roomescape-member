## ✅ 체크 리스트

- [x] 미션의 필수 요구사항을 모두 구현했나요?
- [x] Gradle `test`를 실행했을 때, 모든 테스트가 정상적으로 통과했나요?
- [x] 애플리케이션이 정상적으로 실행되나요?

## 🖥️ 베이스 코드 선택 체크

- [x] 이전 미션의 내 코드에서 시작
    - 기본 베이스는 내 코드
- [x] 이전 미션의 페어의 코드에서 시작
    - 페어의 어노테이션 세팅이나 구조, 테스트 차용

## 🎯 [그룹 규칙](./group_rule.md) 요약

#### 1️⃣ API 설계 선결 조건
- 요구사항에서 드러난 리소스를 식별하고 표현을 결정해야 한다

#### 2️⃣ 서버/클라이언트 검증 책임
- 검증의 책임은 데이터의 종류/성격에 따라 서버/클라이언트 각각에 있다.

#### 3️⃣ 관리자/사용자 API 분리
- 사용자와 관리자의 API 는 분리되어야 한다. 단, 필요한 경우에

#### 4️⃣ 좋은 API, 동작하는 API
- 동작하는 API 중 바람직한 API 가 좋은 API 다.

### `인사말`

처음 뵙겠습니다 썬! 이번 미션의 리뷰이 글렌입니다. 잘 부탁드려요!  
이번 미션의 경우 구현 자체보단 기초적인 개념과 원칙을 학습하는 것이 목적이라 생각해  
크루들과 설계/개념/의도에 대해 많은 이야기를 나누러 노력했어요.

토론 활동에서 API 설계의 관점이 클라이언트/서버로 갈리며  
멘탈 모델과 도메인 모델 중 무엇에 초점을 맞춰야 할 것인지 고민할 수 있었고,  
그룹 규칙을 유연하게 설정하되 미션 진행 과정에서 스스로 확립한다는 합의를 이뤘어요.

이어서 구현과 페어 프로그래밍 과정에서  
도메인 관점과 DB 관점, DDD 에 대한 궁금증과 나름의 답을 내리고 학습 로그에 정리해 보았어요.

### `본문`

<details><summary><h3>🏗️ API 설계와 근거 정리</h3></summary>

## ✅ 1 단계 - 테마 도메인 추가

| 도메인       | Method   | Endpoint       | Path Variable | Query Parameter                      | Request Body                                          | Response              | Description                            |
|:----------|:---------|:---------------|:--------------|:-------------------------------------|:------------------------------------------------------|:----------------------|:---------------------------------------|
| **Theme** | `GET`    | `/themes`      | -             | -                                    | -                                                     | `List<ThemeResponse>` | 모든 테마 목록 조회                            |
| **Theme** | `GET`    | `/themes`      | -             | `topCount` (Long)<br>`during` (Long) | -                                                     | `List<ThemeResponse>` | 기간(`during`) 내 상위(`topCount`) 인기 테마 조회 |
| **Theme** | `POST`   | `/themes`      | -             | -                                    | `ThemeRequest`<br>*(name, description, thumbnailUrl)* | `ThemeResponse`       | 새로운 테마 생성                              |
| **Theme** | `DELETE` | `/themes/{id}` | `id` (Long)   | -                                    | -                                                     | `200 OK` (Void)       | 식별자를 통한 테마 삭제                          |

### API 설계의 근거

- 테마는 `리소스` 라 판단, 단독 엔드포인트로 정의
  - 현재 예약의 필드로만 사용되는 종속적 `도메인` 이지만,
  - 클라이언트의 입장에서 독립된 멘탈 모델, 즉 `리소스` 이다.
- API 설계 원칙에 따라 행위를 HTTP 메서드로 표현

## ✅ 2단계 - 사용자 예약

| 도메인             | Method | Endpoint        | Path Variable | Query Parameter                        | Request Body                                            | Response              | Description            |
|:----------------|:-------|:----------------|:--------------|:---------------------------------------|:--------------------------------------------------------|:----------------------|:-----------------------|
| **Time**        | `GET`  | `/timeSlots`    | -             | `themeId` (long)<br>`date` (LocalDate) | -                                                       | `List<TimeResponse>`  | 특정 날짜, 테마의 예약 가능 시간 조회 |
| **Reservation** | `POST` | `/reservations` | -             | -                                      | `ReservationRequest`<br>*(name, date, timeId, themeId)* | `ReservationResponse` | 새로운 예약 생성              |

### API 설계의 근거

> 사용자가 `날짜와 테마를 선택`하면 예약 가능한 시간 목록이 표시된다

- 사용자가 `선택`한 `날짜`, `테마`를 기반으로 `예약 가능한 시간` 목록을 표시한다
  - 요청 - 날짜, 테마
  - Request Parameter - date, theme(id)
  - 응답 - 시간(예약 가능한)
  - Response Body - list<timeSlot>

> 사용자가 예약 가능한 시간을 선택하여 본인의 이름으로 예약한다 -> POST /reservation

- 사용자가 `선택`한 `날짜`, `테마`, `시간`을 기반으로 예약을 진행한다
  - 요청 - 날짜, 테마, 시간
  - Request Parameter - date, theme(id), timeSlot(id)
  - 응답 - 예약 결과
  - Response Body - reservation

- 테마는 `리소스` 라 판단, 단독 엔드포인트로 정의
  - 현재 예약의 필드로만 사용되는 종속적 `도메인` 이지만,
  - 클라이언트의 입장에서 독립된 멘탈 모델, 즉 `리소스` 이다.
- API 설계 원칙에 따라 행위를 HTTP 메서드로 표현

## ✅ 3단계 - 인기 테마 조회

| 도메인       | Method | Endpoint  | Path Variable | Query Parameter                      | Request Body | Response              | Description                            |
|:----------|:-------|:----------|:--------------|:-------------------------------------|:-------------|:----------------------|:---------------------------------------|
| **Theme** | `GET`  | `/themes` | -             | `topCount` (Long)<br>`during` (Long) | -            | `List<ThemeResponse>` | 기간(`during`) 내 상위(`topCount`) 인기 테마 조회 |

### API 설계의 근거

- 특정 `스코프` 를 기준으로 `리소스` 인 테마를 조회.
  - 테마를 조회한다는 `목적` 과 `횅위` 가 동일하기에 동일한 엔드포인트로
  - 클라이언트의 입장에서 검색 조건인 `스코프` 로 행위를 추론

</details>

## 어떤 부분에 집중하여 리뷰해야 할까요?

### API 설계

`1단계`  
그룹 규칙은 아래와 같지만,
> 만약 사용자의 권한에 따라 응답 필드의 구성이나 노출 데이터가 현저히 다르다면  
> → 도메인 목적에 따라 API를 분리하거나 권한 로직으로 분기한다.

현재 구조에서 권한에 대한 분리가 존재하지 않기에  
1 단계에선 테마에 대한 접근이 관리자 전용임을 표시하기 위해  
/admin 을 붙이기보단 사용자의 테마 접근 가능성을 배제하고  
/theme 엔드포인트를 설정했어요.

`2/3단계`  
2/3 단계에선 별도의 엔드포인트를 설정하기보단  
사용자의 입장에서 조회하려는 `목적` 을 엔드포인트로,  
그에 필요한 `기준` 을 파라미터로 설계했어요.

API 설계 규칙/근거/결과물에 대한 리뷰와  
보완점/생각할 점에 대한 피드백을 받고 싶어요!

---

### DDD 와 실용적 예외

[학습 로그](./study/log-01.md) 에서 정리한 DDD 에 대한 고민과 정의,  
그 내용을 이번 미션에서 적용한 부분에 대한 썬의 의견도 궁금해요!

`엔티티 간의 객체 기반 참조`

```java
public record Reservation(Long id, String name, LocalDate date, TimeSlot timeSlot, Theme theme) {
```

`실무적 효용성을 위한 CQS 원칙 위반`

```java
public Reservation save(Reservation reservation) {
```

### `마치며`

아직 정제가 부족한지 장황해진 느낌이 있네요..  
썬이 남겨주신 피드백을 적극 반영해서  
더 명확한 기준과 생각을 가질 수 있도록 노력하겠습니다!  
바쁘신 와중에도 시간과 노력을 들여 리뷰해 주셔서 진심으로 감사드려요!  
평안한 저녁 되시고 내일도 행복한 하루 되세요!! 😄😄
