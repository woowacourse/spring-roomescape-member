services:
  mysql:
    image: mysql:8.0
    container_name: roomescape-mysql
    ports:
      - "33061:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
    environment:
      TZ: Asia/Seoul
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: roomescape
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
