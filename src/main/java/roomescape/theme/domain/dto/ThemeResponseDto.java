package roomescape.theme.domain.dto;

public record ThemeResponseDto(Long id,
                               String name,
                               String description,
                               String thumbnail) {
}
