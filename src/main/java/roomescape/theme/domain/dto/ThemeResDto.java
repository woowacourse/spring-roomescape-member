package roomescape.theme.domain.dto;

public record ThemeResDto(Long id,
                          String name,
                          String description,
                          String thumbnail) {
}
