spring:
  datasource:
    url: jdbc:h2:mem:roomescape
    username: sa
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true

security:
  jwt:
    token:
      secret-key: P@ssw0rd
      expire-length: 3600000
