spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:database

security:
  jwt:
    token:
      secret-key: markSecretKeymarkSecretKeymarkSecretKeymarkSecretKeymarkSecretKeymarkSecretKey
      expire-length: 3600000
