spring:
  config:
    activate:
      on-profile: "default"
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:database

jwt:
  secret: d29vd2Fjb3Vyc2UtZG9uZ2NoYW4tand0
  expiration:
    access-token: 3600000

---
spring:
  config:
    activate:
      on-profile: "test"
  h2:
    console:
      enabled: true
      path: /h2-console-test
  datasource:
    url: jdbc:h2:mem:database_test
server:
  port: 8081

jwt:
  secret: "testtesttesttesttesttesttesttesttesttest"
  expiration:
    access-token: 1000
