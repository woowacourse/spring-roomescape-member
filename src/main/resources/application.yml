spring:
  datasource:
    url: jdbc:h2:mem:database
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

security:
  jwt:
    token:
      secret-key: "sldkfjlsdghlsdkjflaksj"
      expire-length: 3600000
