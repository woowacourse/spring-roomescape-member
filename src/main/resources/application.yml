server:
  servlet:
    session:
      cookie:
        name: token

spring:
  profiles:
    default: local
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:database
