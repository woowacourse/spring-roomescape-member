spring:
  datasource:
    url: jdbc:h2:mem:databasetest
  h2:
    console:
      enabled: true
      path: /h2-console
#  sql:
#    init:
#      schema-locations: schema.sql
#      data-locations: data.sql
