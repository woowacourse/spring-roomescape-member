server:
  port: 63155

spring:
  datasource:
    url: jdbc:h2:mem:test;DATABASE_TO_LOWER=TRUE
    username: sa

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: embedded

security:
  jwt:
    token:
      secret-key: dobbyismakingjavaspringroomescapemember
      expire-length: 3600000  # 1시간
