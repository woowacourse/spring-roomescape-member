spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:database
    driverClassName: org.h2.Driver
  sql:
    init:
      # 비어 있으면 정상 작동, test-data.sql을 만들고 비워두면 에러.
      data-locations:

security:
  jwt:
    token:
      secret-key: 6f8357b21fda609654fef0431e86f17d9786c9487c32f9cc558b13a882022b21

logging:
  level:
    org.springframework:
      jdbc: DEBUG

