spring:
  datasource:
    url: jdbc:h2:mem:test-database

  sql:
    init:
      platform: h2
      schema-locations: classpath:/schema.sql
      data-locations: classpath:/data.sql

jwt:
  secret: Idontcareifthekeyisstolen
