## 1단계 - 서비스 정책 적용
다음 정책을 만족하지 않는 요청은 거부한다. -> 거부까지만, 어떤 응답을 할 지는 2단계

- [x] 지나간 날짜·시간에 대한 예약 생성은 불가능하다.
  - 날짜, 시간에 대해 입력 받을 때 검증 로직 추가
  - 시간 데이터 자체가 생성이 안되는 것은 아니며, 예약 데이터 생성 시에만 해당이 되어야 한다.
  - 시간만 독립적으로 보는 것이 아니라 날짜 + 시간을 동시에 봐야한다
- [x] 같은 날짜+시간+테마에 이미 예약이 있으면 중복 예약을 거부한다.
  - 이미 프론트 단에서는 available 시간대만 접근 가능
  - postman 등의 도구를 통한 POST 요청에 대한 검증 로직 추가
- [x] 예약이 존재하는 시간을 삭제할 수 없다.
- [x] 유효하지 않은 입력값(빈 이름, 잘못된 날짜 형식 등)을 거부한다.
  - 사이클1에서 만들어둔 입력값 검증
    - 빈 이름 입력 불가
    - 이름 10자 이상 불가
    - 날짜 null 불가
    - theme/time null 불가
    - 잘못된 날짜 형식 일부 불가
  - 추가해야할 입력값 검증
    - [x] themeId, timeId 누락 검증 명시화

## 2단계 - 에러 응답 설계
- [x] 서비스 정책 위반, 유효하지 않은 입력, 존재하지 않는 리소스 등에 대해 의도된 에러 응답을 반환한다.
  - [x] 유효하지 않은 입력 -> 400
  - [x] 서비스 정책 위반 -> 409
  - [x] 존재하지 않는 리소스 -> 404
- [ ] 500(서버 에러)이 사용자에게 노출되지 않도록 한다.
  - [x] 에러 응답 본문에 어떤 정보를 담을지 결정한다.
    - code, http status, message
- [ ] 브라우저에서 에러 발생 시 사용자에게 의미 있는 메시지가 표시되어야 한다.

## 3단계 - 내 예약 조회/변경/취소
- [ ] 사용자가 자신의 이름으로 본인의 예약 목록을 조회할 수 있다.
- [ ] 사용자가 본인의 예약을 취소할 수 있다.
- [ ] 사용자가 본인의 예약의 날짜·시간을 변경할 수 있다.
- [ ] 변경·취소 시 발생하는 에러 케이스(이미 지난 예약을 취소, 변경하려는 시간이 이미 차 있음 등)도 2단계의 규칙에 맞춰 처리한다.


---
# API 설계 규칙

## 1. 좋은 API의 기준

### 1-1. 클라이언트 관점의 API를 설계한다

- 클라이언트 관점에서 이해하기 쉽고 예측 가능해야 한다.
- 서버 구현 방식보다 클라이언트가 어떤 정보를 필요로 하는지를 우선한다.
- 사용 목적과 대상이 명확해야 한다.
    - 예: `/api/admin/**`
    - 예: `/api/**`

---

### 1-2. 일관성을 유지한다

- 동일한 목적의 API는 동일한 패턴으로 동작해야 한다.
- 요청 방식, 응답 구조, 상태 코드, 네이밍 규칙 등을 일관되게 유지한다.
- 클라이언트가 새로운 API를 보더라도 기존 사용 경험을 기반으로 동작을 예측할 수 있어야 한다.

---

# 엔드포인트 설계 규칙

## 2. 리소스 중심으로 엔드포인트를 설계한다

### 2-1. 리소스 식별 기준

- 클라이언트 관점에서 필요한 핵심 데이터를 기준으로 리소스를 식별한다.
- 내부 구현이나 데이터베이스 구조보다 사용자가 다루는 개념을 우선한다.

---

### 2-2. 사용자 의도가 드러나는 구조를 사용한다

- URI만 보고도 어떤 리소스를 다루는지 이해 가능해야 한다.
- 가능한 한 리소스를 중심으로 표현한다.
- 서버 내부 동작이나 절차를 직접 드러내는 표현은 지양한다.

---

# 에러 처리 설계 규칙

## 3. 에러 응답은 클라이언트의 다음 행동을 도와야 한다

### 3-1. 에러 응답의 수신자를 구분한다

- `message`
    - 사용자(또는 API 소비자)를 위한 정보로 제공한다.
- `code`, `http status`
    - 프론트엔드/클라이언트가 에러를 식별하고 분기 처리하기 위한 정보로 사용한다.

---

### 3-2. 비즈니스 규칙 위반에 대한 응답 기준을 정의한다

- 비즈니스 예외 응답 방식은 팀 내 합의된 기준을 우선한다.
- 프론트엔드와 협의하여 UX/UI 관점에서 필요한 정보 수준을 결정한다.
- 사용자가 수행할 수 없는 행동이라면 그 이유를 명확히 전달한다.

예시:

- 이미 지난 예약은 취소할 수 없으므로 → 취소 불가 응답 반환

---

### 3-3. 필요한 경우 응답에 다음 행동을 포함한다

아래 조건을 모두 고려했을 때 필요한 경우에만 포함한다.

- 사용자의 요청/입력으로 인해 발생한 문제인가?
- 사용자가 다음에 무엇을 해야 할지 응답만으로 알기 어려운가?
- 제공하는 정보가 실제로 사용자의 다음 행동 결정에 도움이 되는가?


---
### 의문사항과 내 생각 기록
- 500은 아예 노출이 되면 안되는가? 오로지 서버 개발자가 확인하기 위한 용도?
  - 500은 보통 예상하지 못한 서버 에러 상황에 가깝다.(DB 연결, 예상 못한 런타임 오류, 외부 API 장애 등)
  - 이번 미션에서는 500을 사용자에게 노출되지 않도록 한다는 요구사항이 존재한다.
    - 정책에 대한 예외, 사용자의 잘못된 접근에 의한 예외를 500으로서 보이지 않게 하고 의도된 예외 코드로서 메시지를 전달하도록 하는 것이 목표이지, 500 코드가 아예 보이면 안된다는 의미는 아닌 것으로 보인다.
- '브라우저에서 에러 발생 시 사용자에게 의미 있는 메시지가 표시되어야 한다.' 여기서 사용자는 Client? Frontend?
  - 백엔드 입장에서는 프론트를, 프론트 입장에선 사용자를 고려한 메시지를 전달해야 한다고 생각한다.
    - 좋은 메시지의 기준으로 잡히는 '사용자가 문제 해결법을 예측할 수 있도록', '부정적 표현을 삼가' 등의 기준은 브라우저에서 사용자가 보는 메시지에 해당되는 기준
    - 따라서, 실제 사용자가 보는 메시지를 생성하는 프론트 측에서 UI/UX를 고려하여 작성해야 하는 메시지를 말함
    - 백엔드는 프론트가 그러한 메시지를 작성할 수 있도록, 정확한 원인을 파악할 수 있는 예외 정보를 전달
    - 프론트 단에서는 백엔드로부터 명확한 정보를 받아서, 사용자에게 전달할 메시지를 구성하여 화면에 내보낸다.
- 
